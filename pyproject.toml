[project]
name = "four-in-a-row"
version = "0.1.0"
description = "Four-in-a-row rules engine, players, and structured match logging."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
